fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
aiosqlite==0.20.0
httpx==0.27.2

# --- MultiHaluDet branch (hidden-state trajectory probing) ---
# Replaces Ollama for generation: Ollama's HTTP API has no way to expose
# per-layer hidden states / logits, which the base-paper method requires.
# CPU-only by default (multihaludet_device="cpu" in config/settings.py);
# install a CUDA build of torch instead if you have a GPU.
torch==2.11.0
transformers==4.45.2
accelerate==0.34.2
numpy==1.26.4
scikit-learn==1.5.2

# --- Optional but recommended for full quality (Phases 6-7) ---
# Rule-based fallbacks are used automatically if these aren't installed.
# spacy==3.7.5
# (after installing spacy: python -m spacy download en_core_web_sm)
# sentence-transformers==3.1.1
# faiss-cpu==1.8.0.post1
